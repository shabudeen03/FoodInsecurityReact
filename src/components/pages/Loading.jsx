/*
    Fallback for when await/fetch will be utilized to notify user while the data is loading
*/

function Loading() {
    return (<>
        <h1>Loading ...</h1>
    </>);
}

export default Loading;